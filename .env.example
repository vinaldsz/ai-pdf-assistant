# Example environment variables for local development.
# Copy this file to `.env` and fill in real secret values. Do NOT commit `.env`.

# Provider API keys
GROQ_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
PHI_API_KEY=

# PostgreSQL / pgvector connection (used by pdf_assistant.py)
# Either provide a full SQLAlchemy URL or individual connection vars.
DB_URL=postgresql+psycopg://ai:ai@localhost:5532/ai
# Or use these components instead of DB_URL:
# PGHOST=localhost
# PGPORT=5532
# PGUSER=ai
# PGPASSWORD=ai
# PGDATABASE=ai
